##Проект по отображении шести часов, с разными часовыми поясами.

Для сборки проекта нужно, зайти в папку build:
`cd work_clock`
`cd build`

Сборка проекта через cmake:
`cmake ..`
`cmake --build .`

Сборка через make:
`make`

Запуск проекта:
`./clock`
